<Window
    x:Class="MelhorWindows.Desktop.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="MelhorWindows"
    Width="980"
    Height="780"
    MinWidth="940"
    MinHeight="700"
    AllowsTransparency="True"
    Background="Transparent"
    Icon="Assets/AppIcon.ico"
    ResizeMode="NoResize"
    SnapsToDevicePixels="True"
    TextOptions.TextFormattingMode="Display"
    TextOptions.TextRenderingMode="ClearType"
    UseLayoutRounding="True"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None">
    <Window.Resources>
        <SolidColorBrush x:Key="BgBrush" Color="#060E20" />
        <SolidColorBrush x:Key="PanelBrush" Color="#091634" />
        <SolidColorBrush x:Key="PanelAltBrush" Color="#0D214C" />
        <SolidColorBrush x:Key="DeepPanelBrush" Color="#071127" />
        <SolidColorBrush x:Key="WellBrush" Color="#050C1C" />
        <SolidColorBrush x:Key="TextBrush" Color="#DEE5FF" />
        <SolidColorBrush x:Key="MutedBrush" Color="#91AAEB" />
        <SolidColorBrush x:Key="SoftBrush" Color="#5B74B1" />
        <SolidColorBrush x:Key="BorderBrushSoft" Color="#1E3768" />
        <SolidColorBrush x:Key="PrimaryBrush" Color="#C0C1FF" />
        <SolidColorBrush x:Key="PrimaryFgBrush" Color="#2522B0" />
        <LinearGradientBrush x:Key="ShellBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Offset="0" Color="#0A1B40" />
            <GradientStop Offset="1" Color="#08142A" />
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="PrimaryButtonBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Offset="0" Color="#D9D7FF" />
            <GradientStop Offset="1" Color="#B5B7FF" />
        </LinearGradientBrush>
        <DropShadowEffect x:Key="ShellShadow" BlurRadius="42" Color="#000000" Opacity="0.42" ShadowDepth="0" />

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="FontFamily" Value="Segoe UI Variable Text" />
        </Style>
        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource MutedBrush}" />
            <Setter Property="FontFamily" Value="Segoe UI Variable Display" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Background" Value="{StaticResource PanelAltBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushSoft}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontFamily" Value="Segoe UI Variable Text" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Height" Value="42" />
            <Setter Property="MinWidth" Value="118" />
            <Setter Property="Padding" Value="16,0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            x:Name="Chrome"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="18">
                            <ContentPresenter Margin="{TemplateBinding Padding}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="#143066" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Chrome" Property="Opacity" Value="0.92" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Chrome" Property="Opacity" Value="0.55" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Foreground" Value="{StaticResource PrimaryFgBrush}" />
            <Setter Property="Background" Value="{StaticResource PrimaryButtonBrush}" />
            <Setter Property="BorderBrush" Value="#D2D2FF" />
            <Setter Property="FontWeight" Value="Bold" />
        </Style>
        <Style x:Key="GhostButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
        </Style>
        <Style x:Key="RoundIconButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Width" Value="42" />
            <Setter Property="MinWidth" Value="42" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Background" Value="#0E2149" />
            <Setter Property="BorderBrush" Value="#26477E" />
        </Style>
        <Style x:Key="NavButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="MinWidth" Value="0" />
            <Setter Property="Height" Value="44" />
        </Style>
        <Style x:Key="ActiveNavButtonStyle" TargetType="Button" BasedOn="{StaticResource NavButtonStyle}">
            <Setter Property="Background" Value="#112A59" />
            <Setter Property="BorderBrush" Value="#32569F" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Background" Value="#0B2047" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushSoft}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,8" />
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Background" Value="#0B2047" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushSoft}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,6" />
        </Style>
        <Style x:Key="RecentItemStyle" TargetType="ListBoxItem">
            <Setter Property="Margin" Value="0,0,14,0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Tile" Width="92" Height="92" Background="#0B1E44" BorderBrush="#183664" BorderThickness="1" CornerRadius="18">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Tile" Property="Background" Value="#102856" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Tile" Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="LibraryItemStyle" TargetType="ListBoxItem">
            <Setter Property="Margin" Value="0,0,12,12" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Tile" Width="66" Height="66" Background="#0B1A39" BorderBrush="#163261" BorderThickness="1" CornerRadius="14">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Tile" Property="Background" Value="#122A59" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Tile" Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="Transparent">
        <Border
            Width="860"
            MaxHeight="760"
            Margin="24"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Background="{StaticResource ShellBrush}"
            BorderBrush="#173265"
            BorderThickness="1"
            CornerRadius="26"
            Effect="{StaticResource ShellShadow}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Border
                    Grid.Row="0"
                    Padding="26,20"
                    Background="#101D42"
                    BorderBrush="#173265"
                    BorderThickness="0,0,0,1"
                    CornerRadius="26,26,0,0"
                    MouseLeftButtonDown="WindowHeader_MouseLeftButtonDown">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock
                                x:Name="PageTitleTextBlock"
                                FontFamily="Segoe UI Variable Display"
                                FontSize="26"
                                FontWeight="Bold"
                                Text="Trocar icone" />
                            <TextBlock
                                x:Name="PageSubtitleTextBlock"
                                Margin="0,8,0,0"
                                Foreground="{StaticResource MutedBrush}"
                                Text="Selecione uma imagem, revise o resultado e aplique na pasta atual." />
                        </StackPanel>
                        <Button
                            Grid.Column="1"
                            Margin="0,0,10,0"
                            Click="AccountButton_Click"
                            Style="{StaticResource RoundIconButtonStyle}">
                            <TextBlock
                                FontFamily="Segoe MDL2 Assets"
                                FontSize="18"
                                Foreground="{StaticResource PrimaryBrush}"
                                Text="&#xE77B;" />
                        </Button>
                        <Button
                            Grid.Column="2"
                            Click="WindowCloseButton_Click"
                            Style="{StaticResource RoundIconButtonStyle}">
                            <TextBlock
                                FontFamily="Segoe MDL2 Assets"
                                FontSize="18"
                                Foreground="{StaticResource MutedBrush}"
                                Text="&#xE711;" />
                        </Button>
                    </Grid>
                </Border>

                <Border
                    x:Name="StatusBorder"
                    Grid.Row="1"
                    Margin="30,16,30,0"
                    Padding="14,12"
                    Background="#102A22"
                    BorderBrush="#2A5946"
                    BorderThickness="1"
                    CornerRadius="14"
                    Visibility="Collapsed">
                    <TextBlock x:Name="StatusTextBlock" Foreground="#A5F4CB" TextWrapping="Wrap" />
                </Border>

                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" CanContentScroll="False">
                    <StackPanel x:Name="QuickChangeHost" Margin="24,18,24,14">
                        <Border Padding="16,14" Background="{StaticResource PanelBrush}" CornerRadius="20">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Margin="0,0,18,0">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Pasta atual" />
                                        <TextBlock x:Name="CurrentFolderNameTextBlock" Margin="12,0,0,0" FontFamily="Segoe UI Variable Display" FontSize="17" FontWeight="Bold" TextWrapping="Wrap" />
                                    </StackPanel>
                                    <TextBlock x:Name="FolderPathTextBlock" Margin="0,8,0,0" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" />
                                    <TextBlock x:Name="PreviewFolderNameTextBlock" Visibility="Collapsed" />
                                </StackPanel>
                                <Button Grid.Column="1" Width="128" Margin="12,0,0,0" Click="ChooseFolderButton_Click" Content="Escolher pasta" />
                            </Grid>
                        </Border>

                        <Grid Margin="0,18,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="186" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Enviar icone customizado" />
                                <Border Margin="0,14,0,0" Padding="12" Background="{StaticResource PanelBrush}" CornerRadius="24">
                                    <Grid Height="132" AllowDrop="True" DragOver="UploadDropZone_DragOver" Drop="UploadDropZone_Drop">
                                        <Border Background="{StaticResource WellBrush}" CornerRadius="20" />
                                        <Rectangle Margin="1" RadiusX="20" RadiusY="20" Stroke="#2C467E" StrokeThickness="1.2" StrokeDashArray="6 6" />
                                        <Image x:Name="SourcePreviewImage" Margin="22" Opacity="0.18" Stretch="Uniform" />
                                        <Button
                                            x:Name="ChooseImageButton"
                                            Background="Transparent"
                                            BorderBrush="Transparent"
                                            BorderThickness="0"
                                            Click="ChooseImageButton_Click"
                                            Height="Auto"
                                            MinWidth="0"
                                            Padding="0"
                                            Style="{StaticResource GhostButtonStyle}">
                                            <Grid>
                                                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                                    <Border Width="52" Height="52" HorizontalAlignment="Center" Background="{StaticResource PrimaryButtonBrush}" CornerRadius="26">
                                                        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontFamily="Segoe MDL2 Assets" FontSize="22" Foreground="{StaticResource PrimaryFgBrush}" Text="&#xE710;" />
                                                    </Border>
                                                    <TextBlock x:Name="SourcePreviewPlaceholderTextBlock" Margin="0,12,0,0" HorizontalAlignment="Center" FontSize="14" FontWeight="SemiBold" Text="Arraste um arquivo ou clique para carregar" />
                                                    <TextBlock Margin="0,6,0,0" HorizontalAlignment="Center" Foreground="{StaticResource SoftBrush}" Text="SVG, PNG, JPG, WEBP, GIF, TIFF ou ICO" />
                                                </StackPanel>
                                            </Grid>
                                        </Button>
                                    </Grid>
                                </Border>
                            </StackPanel>

                            <Border Grid.Column="1" Margin="16,28,0,0" Padding="14" Background="{StaticResource PanelBrush}" CornerRadius="22">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Acoes" />
                                    <Button x:Name="ApplyIconButton" Margin="0,12,0,0" Click="ApplyIconButton_Click" Content="Aplicar" Style="{StaticResource PrimaryButtonStyle}" />
                                    <Button Margin="0,10,0,0" Click="CancelButton_Click" Content="Cancelar" />
                                    <Border x:Name="SelectionSummaryCard" Margin="0,14,0,0" Padding="12" Background="{StaticResource DeepPanelBrush}" CornerRadius="18" Visibility="Collapsed">
                                        <StackPanel>
                                            <Border Width="82" Height="82" HorizontalAlignment="Center" Background="#0A1730" CornerRadius="18">
                                                <Grid>
                                                    <Image x:Name="GeneratedPreviewImage" Margin="14" Stretch="Uniform" />
                                                    <TextBlock x:Name="GeneratedPreviewPlaceholderTextBlock" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{StaticResource MutedBrush}" Text="Previa" />
                                                </Grid>
                                            </Border>
                                            <TextBlock x:Name="SelectedImageNameTextBlock" Margin="0,12,0,0" FontFamily="Segoe UI Variable Display" FontSize="16" FontWeight="Bold" TextAlignment="Center" TextWrapping="Wrap" />
                                            <TextBlock x:Name="SelectedImageMetaTextBlock" Margin="0,8,0,0" Foreground="{StaticResource MutedBrush}" FontSize="12" TextAlignment="Center" TextWrapping="Wrap" />
                                            <TextBlock x:Name="PreviewModeTextBlock" Margin="0,8,0,0" Foreground="{StaticResource SoftBrush}" FontSize="12" TextAlignment="Center" TextWrapping="Wrap" />
                                            <Button x:Name="ReplaceImageButton" Margin="0,12,0,0" Click="ChooseImageButton_Click" Content="Trocar imagem" />
                                            <Button x:Name="UpdatePreviewButton" Margin="0,10,0,0" Click="UpdatePreviewButton_Click" Content="Atualizar" Visibility="Collapsed" />
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </Border>
                        </Grid>

                        <Border x:Name="ImageAdjustmentCard" Margin="0,20,0,0" Padding="18" Background="{StaticResource PanelBrush}" CornerRadius="22" Visibility="Collapsed">
                            <StackPanel>
                                <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Ajuste de enquadramento" />
                                <TextBlock x:Name="AdjustmentHintTextBlock" Margin="0,10,0,0" FontFamily="Segoe UI Variable Display" FontSize="18" FontWeight="Bold" TextWrapping="Wrap" />
                                <Grid Margin="0,16,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="260" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Foreground="{StaticResource MutedBrush}" Text="Modo" />
                                        <ComboBox x:Name="FitModeComboBox" Margin="0,8,0,0" SelectedIndex="0" SelectionChanged="FitModeComboBox_SelectionChanged">
                                            <ComboBoxItem Content="Enquadrar ao quadrado" Tag="CropToSquare" />
                                            <ComboBoxItem Content="Mostrar imagem completa" Tag="FitInsideSquare" />
                                        </ComboBox>
                                    </StackPanel>
                                    <CheckBox x:Name="ManualCropCheckBox" Grid.Column="1" Margin="22,30,0,0" Checked="ManualCropCheckBox_Changed" Unchecked="ManualCropCheckBox_Changed" Content="Ajuste fino do corte" />
                                </Grid>
                                <Border x:Name="ManualCropPanel" Margin="0,16,0,0" Padding="16" Background="{StaticResource DeepPanelBrush}" CornerRadius="18" Visibility="Collapsed">
                                    <WrapPanel>
                                        <StackPanel Margin="0,0,12,12"><TextBlock Foreground="{StaticResource MutedBrush}" Text="X" /><TextBox x:Name="CropXTextBox" Width="96" Margin="0,6,0,0" LostFocus="CropField_LostFocus" /></StackPanel>
                                        <StackPanel Margin="0,0,12,12"><TextBlock Foreground="{StaticResource MutedBrush}" Text="Y" /><TextBox x:Name="CropYTextBox" Width="96" Margin="0,6,0,0" LostFocus="CropField_LostFocus" /></StackPanel>
                                        <StackPanel Margin="0,0,12,12"><TextBlock Foreground="{StaticResource MutedBrush}" Text="Largura" /><TextBox x:Name="CropWidthTextBox" Width="96" Margin="0,6,0,0" LostFocus="CropField_LostFocus" /></StackPanel>
                                        <StackPanel Margin="0,0,12,12"><TextBlock Foreground="{StaticResource MutedBrush}" Text="Altura" /><TextBox x:Name="CropHeightTextBox" Width="96" Margin="0,6,0,0" LostFocus="CropField_LostFocus" /></StackPanel>
                                        <Button Margin="0,24,0,0" Click="CenterCropButton_Click" Content="Centralizar" />
                                    </WrapPanel>
                                </Border>
                            </StackPanel>
                        </Border>

                        <StackPanel Margin="0,16,0,0">
                            <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Icones recentes" />
                            <ListBox x:Name="HistoryListBox" Margin="0,14,0,0" Background="Transparent" BorderThickness="0" SelectionChanged="HistoryListBox_SelectionChanged">
                                <ListBox.ItemsPanel><ItemsPanelTemplate><StackPanel Orientation="Horizontal" /></ItemsPanelTemplate></ListBox.ItemsPanel>
                                <ListBox.ItemContainerStyle><StaticResource ResourceKey="RecentItemStyle" /></ListBox.ItemContainerStyle>
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Width="64" Height="64">
                                            <Border Width="52" Height="52" HorizontalAlignment="Center" VerticalAlignment="Center" Background="#101D38" CornerRadius="12">
                                                <Image Source="{Binding PreviewImage}" Stretch="Uniform" />
                                            </Border>
                                        </Grid>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                            <Border x:Name="HistoryEmptyStateBorder" Margin="0,12,0,0" Padding="16" Background="{StaticResource PanelBrush}" CornerRadius="16" Visibility="Collapsed">
                                <TextBlock Foreground="{StaticResource MutedBrush}" Text="Os icones aplicados por voce aparecerao aqui para reutilizacao rapida." />
                            </Border>
                        </StackPanel>

                        <StackPanel Margin="0,18,0,0">
                            <Grid Margin="0,0,0,14">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="240" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Biblioteca de icones" />
                                <Grid Grid.Column="1">
                                    <TextBox x:Name="IconLibrarySearchTextBox" Padding="38,8,14,8" TextChanged="IconLibrarySearchTextBox_TextChanged" />
                                    <TextBlock Margin="14,0,0,0" VerticalAlignment="Center" FontFamily="Segoe MDL2 Assets" Foreground="{StaticResource SoftBrush}" Text="&#xE721;" />
                                </Grid>
                            </Grid>
                            <ListBox x:Name="IconLibraryListBox" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Disabled" SelectionChanged="IconLibraryListBox_SelectionChanged">
                                <ListBox.ItemsPanel><ItemsPanelTemplate><UniformGrid Columns="8" /></ItemsPanelTemplate></ListBox.ItemsPanel>
                                <ListBox.ItemContainerStyle><StaticResource ResourceKey="LibraryItemStyle" /></ListBox.ItemContainerStyle>
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Width="52" Height="52">
                                            <Border Width="40" Height="40" HorizontalAlignment="Center" VerticalAlignment="Center" Background="{Binding TileBackgroundBrush}" CornerRadius="11">
                                                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontFamily="Segoe MDL2 Assets" FontSize="18" Foreground="White" Text="{Binding Glyph}" />
                                            </Border>
                                        </Grid>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>

                <Border Grid.Row="3" Height="0" Visibility="Collapsed" />
            </Grid>
        </Border>

        <Grid x:Name="DashboardOverlayRoot" Visibility="Collapsed">
            <Border Background="#7F030915" />
            <Border
                x:Name="DashboardPanel"
                Width="430"
                Margin="0,34,34,34"
                HorizontalAlignment="Right"
                VerticalAlignment="Stretch"
                Background="{StaticResource PanelBrush}"
                BorderBrush="{StaticResource BorderBrushSoft}"
                BorderThickness="1"
                CornerRadius="24"
                Effect="{StaticResource ShellShadow}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Border Padding="22,20" Background="#101D42" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="0,0,0,1" CornerRadius="24,24,0,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal">
                                <Border Width="42" Height="42" Background="#112754" CornerRadius="21">
                                    <TextBlock x:Name="UserInitialsTextBlock" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="17" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" />
                                </Border>
                                <StackPanel Margin="12,1,0,0">
                                    <TextBlock x:Name="UserNameTextBlock" FontSize="16" FontWeight="SemiBold" />
                                    <TextBlock x:Name="UserRoleSummaryTextBlock" Margin="0,4,0,0" Foreground="{StaticResource MutedBrush}" />
                                </StackPanel>
                            </StackPanel>
                            <Button Grid.Column="1" Click="DashboardCloseButton_Click" Style="{StaticResource RoundIconButtonStyle}">
                                <TextBlock FontFamily="Segoe MDL2 Assets" FontSize="16" Foreground="{StaticResource MutedBrush}" Text="&#xE711;" />
                            </Button>
                        </Grid>
                    </Border>

                    <StackPanel Grid.Row="1" Margin="18,18,18,0">
                        <Button x:Name="HomeNavButton" Click="HomeNavButton_Click" Style="{StaticResource ActiveNavButtonStyle}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock FontFamily="Segoe MDL2 Assets" FontSize="16" Foreground="{StaticResource PrimaryBrush}" Text="&#xE80F;" />
                                <TextBlock Margin="12,0,0,0" VerticalAlignment="Center" FontWeight="SemiBold" Text="Painel" />
                            </StackPanel>
                        </Button>
                        <Button x:Name="HistoryNavButton" Margin="0,10,0,0" Click="HistoryNavButton_Click" Style="{StaticResource NavButtonStyle}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock FontFamily="Segoe MDL2 Assets" FontSize="16" Foreground="{StaticResource PrimaryBrush}" Text="&#xE81C;" />
                                <TextBlock Margin="12,0,0,0" VerticalAlignment="Center" FontWeight="SemiBold" Text="Historico" />
                            </StackPanel>
                        </Button>
                        <Button x:Name="SettingsNavButton" Margin="0,10,0,0" Click="SettingsNavButton_Click" Style="{StaticResource NavButtonStyle}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock FontFamily="Segoe MDL2 Assets" FontSize="16" Foreground="{StaticResource PrimaryBrush}" Text="&#xE713;" />
                                <TextBlock Margin="12,0,0,0" VerticalAlignment="Center" FontWeight="SemiBold" Text="Configuracoes" />
                            </StackPanel>
                        </Button>
                    </StackPanel>

                    <Grid Grid.Row="2" Margin="18,18,18,18">
                        <Grid x:Name="HomeView">
                            <StackPanel>
                                <Border Padding="18" Background="#0B1A39" CornerRadius="18">
                                    <StackPanel>
                                        <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Dashboard do projeto" />
                                        <TextBlock x:Name="RolesTextBlock" Margin="0,10,0,0" FontFamily="Segoe UI Variable Display" FontSize="20" FontWeight="Bold" />
                                        <TextBlock x:Name="SettingsSummaryTextBlock" Margin="0,8,0,0" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" />
                                    </StackPanel>
                                </Border>
                                <Border Margin="0,16,0,0" Padding="18" Background="#0B1A39" CornerRadius="18">
                                    <TextBlock Foreground="{StaticResource MutedBrush}" Text="A janela principal fica dedicada a troca rapida de icone. Este painel existe para historico completo, integracoes e recursos administrativos futuros." TextWrapping="Wrap" />
                                </Border>
                            </StackPanel>
                        </Grid>

                        <ScrollViewer x:Name="HistoryView" Visibility="Collapsed" VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Historico completo" />
                                <ItemsControl Margin="0,14,0,0" ItemsSource="{Binding ElementName=HistoryListBox, Path=ItemsSource}">
                                    <ItemsControl.ItemsPanel><ItemsPanelTemplate><WrapPanel /></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Width="176" Margin="0,0,14,14" Padding="16" Background="#0B1A39" CornerRadius="18">
                                                <StackPanel>
                                                    <Border Height="112" Background="#09162E" CornerRadius="14">
                                                        <Image Source="{Binding PreviewImage}" Margin="18" Stretch="Uniform" />
                                                    </Border>
                                                    <TextBlock Margin="0,14,0,0" FontWeight="SemiBold" Text="{Binding Title}" />
                                                    <TextBlock Margin="0,6,0,0" Foreground="{StaticResource MutedBrush}" Text="{Binding Subtitle}" />
                                                    <TextBlock Margin="0,4,0,0" Foreground="{StaticResource SoftBrush}" Text="{Binding AppliedText}" />
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </ScrollViewer>

                        <ScrollViewer x:Name="SettingsView" Visibility="Collapsed" VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <Border x:Name="IntegrationSettingsCard" Margin="0,0,0,16" Padding="18" Background="#0B1A39" CornerRadius="18" Visibility="Collapsed">
                                    <StackPanel>
                                        <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Explorer" />
                                        <TextBlock Margin="0,10,0,0" FontFamily="Segoe UI Variable Display" FontSize="18" FontWeight="Bold" Text="Integracao com clique direito" />
                                        <TextBlock Margin="0,8,0,0" Foreground="{StaticResource MutedBrush}" Text="Registre ou remova o atalho do MelhorWindows no Explorer." TextWrapping="Wrap" />
                                        <WrapPanel Margin="0,18,0,0">
                                            <Button Style="{StaticResource PrimaryButtonStyle}" Click="RegisterVerbButton_Click" Content="Registrar no Explorer" />
                                            <Button Margin="12,0,0,0" Click="UnregisterVerbButton_Click" Content="Remover do Explorer" />
                                        </WrapPanel>
                                    </StackPanel>
                                </Border>
                                <Border x:Name="AdminFeatureSettingsCard" Margin="0,0,0,16" Padding="18" Background="#0B1A39" CornerRadius="18" Visibility="Collapsed">
                                    <StackPanel>
                                        <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Admin" />
                                        <TextBlock Margin="0,10,0,0" FontFamily="Segoe UI Variable Display" FontSize="18" FontWeight="Bold" Text="Recursos do Windows" />
                                        <WrapPanel Margin="0,16,0,12">
                                            <Button Style="{StaticResource PrimaryButtonStyle}" Click="EnableFeatureButton_Click" Content="Habilitar selecionada" />
                                            <Button Margin="12,0,0,0" Click="DisableFeatureButton_Click" Content="Desabilitar selecionada" />
                                            <Button Margin="12,0,0,0" Click="RefreshFeaturesButton_Click" Content="Atualizar lista" />
                                        </WrapPanel>
                                        <ListView x:Name="FeatureListView"><ListView.ItemTemplate><DataTemplate><TextBlock Text="{Binding}" TextWrapping="Wrap" /></DataTemplate></ListView.ItemTemplate></ListView>
                                    </StackPanel>
                                </Border>
                                <Border x:Name="AdminAuditSettingsCard" Margin="0,0,0,16" Padding="18" Background="#0B1A39" CornerRadius="18" Visibility="Collapsed">
                                    <StackPanel>
                                        <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Auditoria" />
                                        <TextBlock Margin="0,10,0,0" FontFamily="Segoe UI Variable Display" FontSize="18" FontWeight="Bold" Text="Historico administrativo" />
                                        <Button Margin="0,16,0,12" Click="RefreshAuditButton_Click" Content="Atualizar auditoria" />
                                        <ListView x:Name="AuditListView"><ListView.ItemTemplate><DataTemplate><TextBlock Text="{Binding}" TextWrapping="Wrap" /></DataTemplate></ListView.ItemTemplate></ListView>
                                    </StackPanel>
                                </Border>
                                <Border x:Name="UserSettingsInfoCard" Padding="18" Background="#0B1A39" CornerRadius="18" Visibility="Collapsed">
                                    <TextBlock Foreground="{StaticResource MutedBrush}" Text="Este perfil vera apenas recursos relevantes. Novos modulos podem ser ligados aqui sem poluir a janela principal." TextWrapping="Wrap" />
                                </Border>
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
